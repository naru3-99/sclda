c言語で、特定の構造体を文字列に変換するためのヘルパー関数を作成してください。ただし、次の構造体を対象にしてください。
-------------

-------------
また、次の要件を守ってください。
- 引数は、その構造体のポインタと、文字列を格納するためのバッファ
- 返り値は、作成した文字列の長さ
- 構造体のメンバ変数が複数ある場合でも、snprintfを一度だけ使用し、一度で文字列に変換すること
- 各メンバ変数は、デリミタ'SCLDA_DELIMITER'で区切ること
また、上記を実装した関数のみを出力し、main関数や対象の構造体等は含めないでください。